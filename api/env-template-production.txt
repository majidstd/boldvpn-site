# BoldVPN Production Configuration
# Copy this to .env and update the values

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=radius
DB_USER=radiususer
DB_PASSWORD=your_db_password_here

# JWT Configuration (MUST be at least 32 characters)
JWT_SECRET=your_very_long_random_secret_key_at_least_32_characters_long_here

# Server Configuration
PORT=3000
NODE_ENV=production
FRONTEND_URL=https://boldvpn.net

# OPNsense API Configuration (REQUIRED for device management)
OPNSENSE_HOST=192.168.50.1
OPNSENSE_PORT=443
OPNSENSE_API_KEY=your_api_key_from_opnsense_gui
OPNSENSE_API_SECRET=your_api_secret_from_opnsense_gui
WIREGUARD_INTERFACE=wg0

# Email Configuration (Optional - for password reset)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password

# Stripe Configuration (Optional - for billing when ready)
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_PRICE_BASIC=price_...
# STRIPE_PRICE_PRO=price_...
# STRIPE_PRICE_PREMIUM=price_...

# To get OPNsense API credentials:
# 1. Login to OPNsense web GUI (https://192.168.50.1)
# 2. Go to: System → Access → Users
# 3. Edit your admin user
# 4. Scroll to "API keys" section
# 5. Click "+" to generate new key
# 6. Copy Key (OPNSENSE_API_KEY) and Secret (OPNSENSE_API_SECRET)

